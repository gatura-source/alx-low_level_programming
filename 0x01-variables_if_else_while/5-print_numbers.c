#include <stdlib.h>
#include <stdio.h>
/**
 * main - entry point
 * Description: prints 0-9
 * Return: 0 Always
 */

int main(void)
{

	int a;

	for (a = 0; a < 10; a++)
	{
		printf("%d", a);
	}
	putchar(10);
	return  (0);
}
