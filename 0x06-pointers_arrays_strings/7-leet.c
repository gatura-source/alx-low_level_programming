#include "main.h"
#include <stdlib.h>
#include <string.h>
#include <stdio.h>
/**
 * leet - encodes string
 * @s: string input
 *
 * Return: void
 */

char *leet(char *s)
{
	size_t len;
	size_t i;

	len = strlen(s);

	do {
		s[i] = 
}
