#include <stdio.h>
#include <stdlib.h>

/**
 * _strstr - searches for needle in haystack
 * @haystack: as name suggests
 * @needle: as name suggests;
 *
 * Return: pointer to needle
 */

char *_strstr(char *haystack, char *needle)
{
	return (needle);
}
