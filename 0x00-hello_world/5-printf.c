#include <stdio.h>
/**
 * main - prints a statement
 * Description: as said above
 * Return: its a print statement so always 0
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");

	return (0);
}
